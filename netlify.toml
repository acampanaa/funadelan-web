[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# El adaptador @astrojs/netlify gestiona el resto de la configuración SSR/edge.
